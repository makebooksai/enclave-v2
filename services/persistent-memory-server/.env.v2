# ═══════════════════════════════════════════════════════════════════════════════
# Enclave v2 Memory Service - Test Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# ISOLATED from production - uses different ports and database
#
# To use: copy to .env or use with docker-compose.v2.yml
# ═══════════════════════════════════════════════════════════════════════════════

# Edition
MEMORY_EDITION=personal

# PostgreSQL (port 5434 to avoid conflict with prod 5433)
POSTGRES_PORT=5434
POSTGRES_USER=aurora
POSTGRES_PASSWORD=MemoryV2Test2025!
POSTGRES_DB=memory_v2_db

# Qdrant (ports 6335/6336 to avoid conflict with prod 6333/6334)
QDRANT_HTTP_PORT=6335
QDRANT_GRPC_PORT=6336

# Redis (port 6380 to avoid conflict with prod 6379)
REDIS_PORT=6380

# Memory Service API (port 8005 to avoid conflict with prod 8004)
MEMORY_PORT=8005

# Logging
LOG_LEVEL=DEBUG

# STM (if needed)
OLLAMA_URL=http://localhost:11434
STM_MODEL=mistral:7b-instruct-v0.3
TRANSCRIPT_DIR=./transcripts
